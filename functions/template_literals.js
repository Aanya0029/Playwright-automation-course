function greet(name) {
    return`Hi ${name}, how are you?`; //using template literals to print the name
}   
let result = greet("Aanya");
console.log(result); //prints the returned value from the function