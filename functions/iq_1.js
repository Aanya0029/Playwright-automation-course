// if (code >= 400 && code < 500) return "Client error";
//if (code >= 500) return "Server error";
//}

//getStatus(200); //Output: "OK"
//getStatus(404); //Output: "Client error"
//getStatus(500); //Output: "Server error"

/*function logTest(name) {
    console.log(`Running: ${name}`);
    //no return statement, so it will return undefined by default

}
let result = logTest("Login test");
console.log(result); //Output: undefined, because the function does not return anything*/

/*greet("Aanya"); //Output: "Hello, Aanya!"
function greet(name) {
    return `Hello, ${name}!`;
}*/

/*sayHi("Aanya"); //Output: "Hi!"
const sayHi = function(name) {
    return `Hi, ${name}!`;
} //ReferenceError: Cannot access 'sayHi' before initialization */

