const greet = function(name) {
    return `Hi ${name}, how are you?`; //using template literals to print the name
};

let r = greet("Aanya");
console.log(r); //prints the returned value from the function